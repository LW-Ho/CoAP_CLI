import threading

# class restcoap(threading.Thread):
#   def __init__(self, node, resource, query, group=None, target=None, name=None, kwargs=None, verbose=None):
#     threading.Thread.__init__(self, group=group, target=target, name=name, verbose=verbose)


  